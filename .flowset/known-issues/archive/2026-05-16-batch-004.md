# Known Issue Batch 004 — Resolved

- **Date**: 2026-05-16
- **WI**: WI-KI-batch-004-design-system + WI-KI-batch-005-followup
- **Trigger**: 사용자 지적 — OP-01~12 12개 화면이 글로벌 컴포넌트(헤더/사이드바/푸터/아이콘/CSS) 인라인 복사로 일관성 결함 누적 (P1 KI-037)
- **Mode**: 디자인 시스템 SSOT 신규 구축 + 사용자 검수 사이클 후속 정리

## 해소된 이슈 (2건)

### KI-037 (P1) — 디자인 시스템 SSOT 부재 — evaluator PASS 8.61/10

**구축 산출물**:
- `wireframes/_design-system/` 12 파일 (README + 7 spec + tokens.css + components.css + icons.svg + _layout-shell.html)
- `wireframes/_showcase.html` 컴포넌트 시연 (29 섹션)
- 03-components.md Anatomy + Props + Variant matrix 16+ 컴포넌트
- Variable Notation 정책 §1-1 + 텍스트 가변성 §2-1
- components.css 자손 셀렉터 + display:block !important + descendant 누설 차단(empty-state)
- icon-btn 배지 좌측 anchor 정책 + 인접 컴포넌트 gap 16+ 의무
- EmptyState `.ico-empty` 명시 클래스 + flex column 정렬

**evaluator 검증** (general-purpose에 evaluator 룰 임베드):
- 4축: 완성도 8.5 / 정합성 8.7 / 구체성 8.8 / 실행가능성 8.4
- 가중 합계 **8.61/10** PASS
- 6대 의무 모두 충족 (변수화 / 정렬 강제 / descendant 누설 차단 / SSOT 무결성 / 글로벌 컴포넌트 일관성 / React 매핑)

**evaluator 후속 P2 2건 즉시 처리**:
1. 03-components.md L102 "필수" vs components.css L11 "권장" 정책 모순 → "필수 + 컴포넌트 강제값과 일치 의무" 통일
2. _layout-shell.html svg attribute 미명시 → 18건 sed로 추가 + 컴포넌트별 정확 사이즈 (sidebar-item 18 / icon-btn 18 / profile-trigger chevron 14)

### KI-039 (P3) — _showcase.html 누락 컴포넌트 12건 추가

**추가된 시연 섹션**:
- Checkbox / Breadcrumb / Tooltip / Popover / FormRow+FormSection+InfoRow / Sidebar 미니어처 (3 역할)
- Header (AppShell) / Footer / MaintenanceBanner / SessionRow / Code
- **Logo (운영사) + TenantLogo (고객사)** — 사용자 추가 지적

목차(toc) 18 → 29 섹션으로 확장.

## 사용자 검수 사이클 (반복 12회 — 시각 결함 처리)

evaluator는 정렬·정합성 매트릭스에 강하나 시각적 시연 결함은 못 잡음. 사용자 직접 검수에서 발견한 12건 모두 즉시 처리:

| # | 결함 | 원인 | 해결 |
|---|------|------|------|
| 1 | 신규 등록 버튼 + 아이콘이 native 24px로 그려짐 | SVG width/height attribute 누락 + CSS만으로 부족 | components.css 자손 셀렉터 + svg attribute 의무 정책 |
| 2 | _showcase 도메인 텍스트 하드코딩 | "신규 테넌트 등록" 등 도메인 콘텐츠가 시연 인스턴스에 박힘 | `[변수명]` 일괄 변환 + Variable Notation 정책 §1-1 |
| 3 | EmptyState button + 아이콘 흐림 (opacity 0.4) | `.empty-state .ico` descendant 셀렉터가 button 안 svg에도 적용 | descendant → `> svg.ico-empty` direct child + 회귀 방지 주석 |
| 4 | EmptyState 아이콘 좌측 치우침 | `text-align: center` + `display: block` 충돌 | flex column + align-items center |
| 5 | avatar-lg/sm 파란 배경 누락 | sub variant가 `.avatar` 확장 없이 사이즈만 정의 | `.avatar, .avatar-sm, .avatar-lg` 공통 셀렉터로 자체 완성 |
| 6 | 종 + 배지 겹침 (3 / 99+) | 배지 사이즈/위치 + 종 사이즈 미세 조정 | 종 18px + 배지 좌측 anchor (left:24) + min-width 16 |
| 7 | 99+ 배지 옆 IconButton 영역 침범 | 배지가 컨테이너 외부 14px 확장 + gap 8 부족 | header-actions gap 8 → 16 + 인접 gap 16 의무 정책 |
| 8 | 단일 3 vs 99+ 배지 좌측 위치 다름 | transform translate 중심 anchor → 폭 가변 | 좌측 anchor 고정 (left:24) → 첫 글자 위치 동일 |
| 9 | trailingIcon row [← prev] vertical 어긋남 | display:block 안 buttons inline flow → baseline 차이 | 별도 flex wrapper로 align-items center 강제 |
| 10 | ruler 빨간선 안 보임 + IconButton 정렬 | repeating-linear-gradient 자식에 가려짐 + gap 8 | absolute 1px solid 라인 2줄 + gap 16 |
| 11 | Button matrix vertical 정렬 | `<table>`에 `.table` 미사용 → vertical-align baseline | showcase `<style>` `.showcase-section table td/th { vertical-align: middle }` |
| 12 | matrix 두 개 column 위치 어긋남 | table-layout auto → 각 매트릭스 column 자동 폭 | `<colgroup>` 명시 + table-layout: fixed |

**+ 추가**: 운영사/테넌트 로고 컴포넌트 누락 → Logo + TenantLogo 신설 (DB system_settings.brand_logo_url + tenants.logo_url 활용)
**+ 추가**: Tooltip "[label] ⓘ" 호버 안 됨 → SVG는 pseudo-element 미지원, wrapper span에 data-tooltip 적용

## 잔존 P3 (open)

| KI | 처리 시점 |
|----|---------|
| KI-038 | OP-02~12 작업 시 (svg attribute 정합) |
| KI-040 | OP-02~12 작업 시 (wireframes/README.md 갱신) |
| KI-041 | OP-02~12 작업 시 (html/ 구 파일 archive) |

P0/P1/P2 잔존 0건 — Phase 5 게이트 클린.

## 핵심 정책 (변경 금지)

본 batch에서 정립된 정책은 `.flowset/HANDOFF.md §5 핵심 정책 결정` 표 참조:
- 디자인 시스템 SSOT (`_design-system/` 단일 source)
- Variable Notation (showcase는 `[변수]`, 화면 HTML은 한글)
- SVG 정렬 의무 (attribute + 자손 셀렉터)
- IconButton 배지 좌측 anchor + 인접 gap 16+
- Logo / TenantLogo 분리

## 후속 작업 (HANDOFF §3 참조)

1. OP-02~12 일괄 재작성 (디자인 시스템 + i18n + 정렬)
2. TA + EM + CM 일괄 재작성
3. Phase 5 전체 evaluator
4. Phase 1/3/4 batch-005 정합 재평가
