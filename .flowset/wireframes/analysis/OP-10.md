# OP-10 운영 리포트 — Wireframe Analysis

> **소스**: `wireframes/html/OP-10.html` · **PRD**: `prd/domains/operator/OP-10-reports.md`
> **레이아웃**: `_design-system/_layout-shell.html` (active=운영 리포트)
> **패턴**: KPI 6 + Period Selector + Charts 4 + Heatmap + 상위 테넌트

## 1. PRD 매핑

| PRD | 구현 |
|------|------|
| 기간 선택 (월/분기/연간/커스텀) | period-selector 4 chip + 상태별 active |
| KPI 6 (테넌트/MAU/MRR/Churn/티켓/가용성) | kpi-row grid 6 cols |
| 차트 4 (MRR 추이/MAU 추이/플랜 분포/티켓 응대) | chart-row 4 cards (1.5/1.5/1/1) |
| 추가 분석 (heatmap, 매출 상위) | 1.2fr/1fr 2 col grid |
| Excel/PDF 내보내기 | btn-ghost (CM-12 / CM-13) |

## 2. 상태 (5)

1 default(이번달) / 2 quarter / 3 year / 4 custom_range / 5 empty

## 3. i18n

`screens.op-10.{title|period.{month|quarter|year|custom}|kpi.{tenants|mau|mrr|churn|tickets|uptime}|chart.{mrrTrend|mauTrend|planDist|ticketResponseTime|activityHeatmap|topRevenue}}`

## 4. API

```
GET /api/v1/operator/reports/overview?period=month|quarter|year|custom&from&to&compareWith
GET /api/v1/operator/reports/mrr-trend?months=12
GET /api/v1/operator/reports/mau-trend?months=12
GET /api/v1/operator/reports/plan-distribution
GET /api/v1/operator/reports/ticket-response-time
GET /api/v1/operator/reports/activity-heatmap
GET /api/v1/operator/reports/top-revenue?limit=10
POST /api/v1/operator/reports/export                    # Excel/PDF
```

## 5. 검증

- [ ] 5 상태 토글 + period-chip active 변경
- [ ] KPI ▲▼ 배지 색상 (증가=success / 감소=success when churn 감소)
- [ ] 차트 placeholder 4 cards + heatmap + 상위 테이블

## 6. Phase 7 변환

- recharts (LineChart/BarChart/Donut/Bar)
- visx 또는 nivo heatmap (요일×시간)
- TanStack Query staleTime 5min
- 기간 변경 시 invalidate

## 권한 매트릭스 (audit hotfix1 — KI-049 확대 보강)

| 역할 | 권한 |
|------|------|
| operator_super + operator_billing R (운영 리포트 — MRR/MAU) | — |

자세한 권한은 PRD §2 (사용자·권한 섹션) 참조. RLS 정책은 Phase 3 `rls.md` SSOT 정합.
