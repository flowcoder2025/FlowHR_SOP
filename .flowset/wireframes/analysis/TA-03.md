# TA-03 직원 상세 — Wireframe Analysis

> **소스**: `wireframes/html/TA-03.html`
> **PRD**: `prd/domains/tenant-admin/TA-03-employee-detail.md`
> **레이아웃**: `_design-system/_layout-shell.html` (AppShell + tenant 8 메뉴, active=직원)
> **패턴**: Detail + Header Cards + Tabs (component-usage-matrix.json §Detail+Tabs)
> **작성**: 2026-05-17 (G3 wf-v0.3.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| 경로 `/admin/employees/:id` | page-title "직원 상세 — 한직원 (E-0044)" + 브레드크럼 |
| 헤더 카드 (프로필 + 요약) | `.header-cards` (280px + 1fr grid) — `.profile-card` + summary-grid 4 셀 |
| 프로필: 사진 + 이름 + 사번 + 직급/부서/고용형태 + 재직상태 배지 | `.profile-avatar-lg` + `.profile-meta` + `.badge.badge-success` |
| 요약: 잔여연차 / 최근근태 / 결재진행 / 재직기간 | `.summary-grid` 4 셀 |
| 액션: 정보수정 / 휴직 / 퇴사 / 문서업로드 | `.page-action-bar .actions` 4 버튼 (퇴사=btn-danger) |
| 탭 9개 | `.tabs-row` (기본정보/인사정보/계약정보/근태/휴가/급여/문서/결재이력/변경이력) |
| 기본정보 탭 | `.form-section` + `.form-row` (이메일/연락처/주소/비상연락처/가족) |
| 인사정보 탭 | `.form-section` + `.form-row` (사번/부서/직급/고용형태/입사일/수습/근무지/직무) |
| 계약정보 탭 | i-lock 아이콘 (super만 연봉 조회) — Phase 7 권한 게이팅 |
| 변경이력 (audit_logs diff) | Phase 7 timeline 컴포넌트 |
| 민감 정보 변경 2FA | Phase 7 modal |

## 2. 상태 매트릭스 (5)

| 키 | state | 설명 | 컨텐츠 분기 |
|---|-------|------|------------|
| 1 | default | 기본정보 탭 | tab[data-tab="basic"] active + tab-default 본문 |
| 2 | loading | 조회 중 | detail-canvas opacity 0.35 + loading-overlay spinner |
| 3 | filtered | 인사정보 탭 (HR 이상 권한) | tab[data-tab="hr"] active + tab-filtered 본문 |
| 4 | empty | 권한 제한 (manager가 비공개 탭 진입 시도) | i-lock 아이콘 + "조회 권한이 없습니다" + 목록으로 |
| 5 | error | EMPLOYEE_FETCH_FAILED | auth-alert-error + request_id req_c1d4 + 재시도/목록 |

## 3. 디자인 시스템 사용

- 레이아웃: `.app-shell` + `.sidebar` (active=직원) + `.header` + `.content` + `.footer`
- 컴포넌트:
  - `.page-action-bar` + `.summary` (브레드크럼) + `.actions` (4 액션 — btn-ghost / btn-danger)
  - `.card` + `.profile-card` (헤더 카드)
  - `.tabs-row` + `.tab` (탭 9개 — variant `.is-active` SSOT 통일)
  - `.form-section` + `.form-section-header` + `.form-row` + `.label`
  - `.badge.badge-success/muted` (재직 / 변경 불가)
  - `.empty-state` + i-lock (권한 제한 패턴)
  - `.auth-alert.auth-alert-error`
- 페이지 한정 (컴포넌트 재정의 X): `.header-cards` / `.profile-avatar-lg` / `.profile-meta` / `.summary-grid` / `.summary-cell` / `.summary-label` / `.summary-value` / 탭 active 토글 / loading overlay

## 4. i18n 매핑

| 키 | ko | en |
|-----|----|-----|
| `screens.ta-03.title` | 직원 상세 | Employee Detail |
| `screens.ta-03.tab.basic` | 기본정보 | Basic |
| `screens.ta-03.tab.hr` | 인사정보 | HR |
| `screens.ta-03.tab.contract` | 계약정보 | Contract |
| `screens.ta-03.tab.attendance` | 근태 | Attendance |
| `screens.ta-03.tab.leave` | 휴가 | Leave |
| `screens.ta-03.tab.payroll` | 급여 | Payroll |
| `screens.ta-03.tab.doc` | 문서 | Documents |
| `screens.ta-03.tab.approval` | 결재이력 | Approvals |
| `screens.ta-03.tab.audit` | 변경이력 | Audit Log |
| `screens.ta-03.action.edit` | 정보 수정 | Edit |
| `screens.ta-03.action.uploadDoc` | 문서 업로드 | Upload Document |
| `screens.ta-03.action.leaveOfAbsence` | 휴직 처리 | Leave of Absence |
| `screens.ta-03.action.resign` | 퇴사 처리 | Resign |
| `screens.ta-03.empty.noPermission` | 조회 권한이 없습니다 | No permission to view |
| `screens.ta-03.error.fetchFailed` | 직원 상세 조회에 실패했습니다 | Failed to fetch employee |

## 5. API 매핑 (api/tenant.md — TA-03 §7)

```
GET   /api/v1/tenant/employees/:id                  # 종합 (헤더 + 모든 탭 데이터 lazy)
PATCH /api/v1/tenant/employees/:id                  # 기본 / 인사 수정
POST  /api/v1/tenant/employees/:id/leave-of-absence
POST  /api/v1/tenant/employees/:id/resign
POST  /api/v1/tenant/employees/:id/documents        # 업로드 (sub-type)
GET   /api/v1/tenant/employees/:id/audit-logs       # 변경 이력 탭
PATCH /api/v1/tenant/employees/:id/role             # 권한 변경
```

## 6. 권한 매트릭스 (Gherkin 정합)

- `tenant_super`: R/U/D/L 전체 9 탭 (퇴사·삭제 권한 포함)
- `tenant_hr_admin`: R/U/L 9 탭 중 퇴사 불가
- `tenant_manager`: R 소속팀 일부 — 기본정보 / 근태 / 휴가 탭만 (5 탭 비공개) → state=empty (i-lock)
- `employee`: R 본인 일부 (기본정보 + 본인 변경이력 read-only) — `/me/profile`로 redirect 권장
- 민감 정보 (계좌번호) 변경: 2FA 재인증 필수 (Phase 7 modal)

## 7. 검증 (시각 검수)

- [ ] 5 상태 토글 — 기본 (basic 탭) / loading / 인사 (hr 탭) / 권한제한 (i-lock) / error
- [ ] 헤더 카드 — 프로필 카드 (280px) + 요약 카드 (1fr) grid
- [ ] 요약 셀 4 — 잔여연차 / 최근근태 (warning 색상) / 결재진행 / 재직기간
- [ ] 탭 9개 — 계약정보 탭에 i-lock 아이콘 (super 제한)
- [ ] 탭 active variant `.is-active`만 사용 (`.active` 금지) — 본 화면은 body[data-state] CSS로 토글
- [ ] 액션 4 — 정보수정 / 문서업로드 / 휴직 / 퇴사 (btn-danger)
- [ ] form-section / form-row 정렬 (label 120px + value)
- [ ] empty 상태 — i-lock 아이콘 + manager 권한 안내 + "직원 목록으로" 버튼
- [ ] error 상태 — request_id req_c1d4 + 재시도 + 목록

## 8. Phase 7 변환

- shadcn/ui Tabs (orientation=horizontal) + URL ?tab=X 동기화
- react-hook-form (기본/인사 인라인 수정) + zod 검증
- 권한 게이팅: `usePermission(role, tab)` hook — 비공개 탭은 메뉴에서 숨김 또는 disabled
- 민감 정보 변경: shadcn/ui Dialog + 2FA 입력
- 퇴사 처리: confirmation modal + 퇴직일 입력 + 잔여연차 정산 안내
- 근태 / 휴가 탭: TA-05 / EM-04 임베드 (직원 단일 필터)
- 변경이력: Supabase audit_logs 쿼리 + diff 컴포넌트
- Realtime: `realtime:employees:id={id}` 구독 → 헤더 카드 KPI 즉시 갱신

## 9. 의존성

- **선행**: TA-02 직원 목록 (클릭으로 진입)
- **연계**: TA-04 (부서 변경), TA-05/TA-07 (근태/휴가 탭), TA-10 (급여 탭), EM-09 (직원 본인 진입)
- **이벤트**: 변경 시 Realtime broadcast → 자기 자신 + 관련 권한자
