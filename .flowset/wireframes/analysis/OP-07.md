# OP-07 기능 플래그 / 모듈 권한 — Wireframe Analysis

> **소스**: `wireframes/html/OP-07.html` · **PRD**: `prd/domains/operator/OP-07-feature-flags.md`
> **레이아웃**: `_design-system/_layout-shell.html` (active=기능 권한)
> **패턴**: Toggle Table + Filter + Add/Override/History Modal

## 1. PRD 매핑

| PRD | 구현 |
|------|------|
| 8 컬럼 토글 테이블 | colgroup width 분배 + toggle-pill on/off/beta |
| 필터 (모듈/상태/베타) | 상단 filter-row 2 select + 베타만 체크 |
| 기능 추가 모달 (super) | btn-primary i-plus → add_modal (키/라벨/모듈/기본값/플랜/베타) |
| 예외 설정 (테넌트별 ON/OFF) | "설정" 링크 → override_modal (검색 + 토글 리스트) |
| 변경 이력 | btn-ghost i-history → history modal (감사 4행) |
| 변경 미저장 sticky bar | save-bar bottom-center 검정 배경 |

## 2. 상태 (5)

1 default / 2 modified (sticky save-bar) / 3 add_modal / 4 override_modal / 5 history

## 3. i18n

`screens.op-07.title` / `screens.op-07.col.{flag|module|globalState|plans|exceptions|beta|appliedAt}` / `screens.op-07.modal.{add|override|history}.title`

## 4. API

```
GET    /api/v1/operator/feature-flags
POST   /api/v1/operator/feature-flags                       # super
PATCH  /api/v1/operator/feature-flags/:slug                 # 글로벌 토글
PATCH  /api/v1/operator/feature-flags/:slug/tenant/:tid     # 테넌트 예외
GET    /api/v1/operator/feature-flags/:slug/history
```

## 5. 검증

- [ ] 5 상태 토글 + sticky save-bar 검정 배경 + 흰 텍스트
- [ ] toggle-pill 3 variant (on=success / off=muted / beta=warning)
- [ ] 예외 테넌트 모달 — 검색 + 리스트 + ON/OFF 토글

## 권한 매트릭스 (audit hotfix1 — KI-049 확대 보강)

| 역할 | 권한 |
|------|------|
| operator_super CRUD (기능 권한 관리) | — |

자세한 권한은 PRD §2 (사용자·권한 섹션) 참조. RLS 정책은 Phase 3 `rls.md` SSOT 정합.
