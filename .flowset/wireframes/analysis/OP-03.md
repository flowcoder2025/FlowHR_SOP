# OP-03 테넌트 상세 — Wireframe Analysis

> **소스**: `wireframes/html/OP-03.html`
> **PRD**: `prd/domains/operator/OP-03-tenant-detail.md`
> **레이아웃**: `_design-system/_layout-shell.html` (active=테넌트)
> **패턴**: Detail Header + 3 Header Cards + Tabs(8)
> **작성**: 2026-05-16 (G2 wf-v0.2.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| 헤더: 회사명 + 도메인 + 상태 + tenant_id | 상단 h2 + code + .badge-success + muted |
| 핵심 카드 3 (대표관리자/계약/사용량) | `.header-cards` grid 3 cols × `.header-card` |
| 액션 버튼 (관리자/요금제/기능권한/비활성화/청구) | 우측 4 btn (관리자/요금제/기능권한 btn-ghost + 비활성화 btn-danger) |
| 8 탭 (기본정보/사용자/구독/청구/기능권한/사용량/감사로그/티켓) | `.tabs-row` 8 tab links + state별 active 표시 |
| 탭 ?tab= URL 동기화 | applyState(s) → history.replaceState |
| 비활성화 → 사유 입력 + 세션 무효화 | `.modal-overlay` + textarea + btn-danger 확정 |

## 2. 상태 매트릭스 (5)

| 키 | state | 표시 |
|---|-------|------|
| 1 | basic | 기본정보 탭 active (form-row 7 행, 회사명 수정 링크) |
| 2 | usage | 사용량 탭 active (차트 placeholder 4 × Line/Bar/Pie) |
| 3 | audit | 감사로그 탭 active (4 row 테이블) |
| 4 | deactivate | 비활성화 모달 열림 (오버레이 + textarea + 확정) |
| 5 | not_found | TENANT_NOT_FOUND (404 인라인 hero + 목록 CTA) |

(추가 3 탭 — 사용자/구독/청구/기능권한/티켓 — 클릭 placeholder, Phase 7에서 추가 콘텐츠)

## 3. 디자인 시스템 사용

- 레이아웃: 글로벌 sidebar/header/content/footer
- 컴포넌트: `.badge-success`, `.form-row`, `.form-section`, `.card`, `.card-flat`, `.table`, `.btn-ghost`, `.btn-danger`, `.textarea`, `.label-required`
- 페이지 한정: `.header-cards` + `.header-card` (3 cols grid) / `.tabs-row` + `.tab` (수평 탭) / `.tab-content` / `.usage-chart-placeholder` / `.modal-overlay` + `.modal-box` / `.not-found-hero` / `.state-debug`

## 4. i18n 매핑

| 키 (catalog 정합) | ko |
|------------------|----|
| `screens.op-03.title` | 테넌트 상세 |
| `screens.op-03.tab.basic` | 기본정보 |
| `screens.op-03.tab.users` | 사용자 / 조직 |
| `screens.op-03.tab.subscription` | 구독 / 요금제 |
| `screens.op-03.tab.billing` | 청구 |
| `screens.op-03.tab.features` | 기능권한 |
| `screens.op-03.tab.usage` | 사용량 |
| `screens.op-03.tab.audit` | 감사로그 |
| `screens.op-03.tab.tickets` | 티켓 |
| `screens.op-03.action.changeAdmin` | 관리자 변경 |
| `screens.op-03.action.deactivate` | 비활성화 |
| `screens.op-03.deactivate.title` | 테넌트 비활성화 |
| `screens.op-03.deactivate.reason_required` | 비활성화 사유 |
| `screens.op-03.not_found` | 테넌트를 찾을 수 없습니다 |

## 5. API 매핑 (api/operator.md)

```
GET   /api/v1/operator/tenants/:id
PATCH /api/v1/operator/tenants/:id
GET   /api/v1/operator/tenants/:id/usage?period=12months
GET   /api/v1/operator/tenants/:id/feature-flags
GET   /api/v1/operator/tenants/:id/audit-logs?page&filter
GET   /api/v1/operator/tenants/:id/tickets
POST  /api/v1/operator/tenants/:id/deactivate
```

## 6. 권한 매트릭스

- `operator_super`: R/U/D/L (비활성화 가능)
- `operator_staff`: R/U/L (비활성화 버튼 disabled)
- 권한 음성: `tenant_*` /operator/tenants/:id 접근 시 403 (CM-05)

## 7. 검증

- [ ] 5 상태 토글 (1/2/3 탭 / 4 모달 / 5 not_found)
- [ ] 핵심 카드 3 — label-sm + value + meta 정렬
- [ ] 8 탭 active border-bottom 색상 + 폰트 가중
- [ ] 사용량 탭 차트 placeholder 4개 grid 2x2
- [ ] 감사로그 — denied 행 badge-danger
- [ ] 비활성화 모달 — overlay + center + textarea + danger CTA
- [ ] not_found — i-building 회색 hero + 목록 CTA

## 8. Phase 7 변환

- Next.js dynamic route `app/operator/tenants/[id]/page.tsx`
- 탭: `useSearchParams().get('tab')` + history.replaceState
- 모달: shadcn/ui `<Dialog>` + react-hook-form + zod (`deactivateSchema { reason: z.string().min(10) }`)
- 차트: recharts (Phase 7 결정) — LineChart / BarChart / PieChart
- 세션 무효화: Supabase `admin.auth.signOut(userId)` × 테넌트 전체
- Realtime broadcast: 비활성화 후 해당 테넌트 채널 trigger → 클라이언트 `/login?reason=tenant_deactivated`
