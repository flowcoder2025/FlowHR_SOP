# CM-20 PWA 설치 가이드 — Wireframe Analysis

> **소스**: `wireframes/html/CM-20.html`
> **PRD**: `prd/domains/common.md §CM-20` + `prd/02-device-matrix.md`
> **레이아웃**: `_design-system/_layout-auth.html`
> **작성**: 2026-05-16 (G1 wf-v0.1.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| 경로 `/install` | 단일 화면 + 5 디바이스 분기 |
| 우선 노출 — 모바일 첫 진입 + EM-01~04 + localStorage 미설정 | (라우팅/미들웨어 영역, 본 화면은 진입 시 표시만) |
| iOS Safari 16.4+ — 공유 → 홈 화면 추가 + 푸시 안내 | state-ios 카드 + 단계별 안내 |
| Android Chrome — ⋮ → 앱 설치 / 자동 배너 | state-android 카드 |
| Desktop — Tauri 다운로드 | state-desktop + GitHub Releases 링크 |
| iOS 16.4 미만 안내 | state-ios_old + warning alert |
| 이미 PWA 진입 — 자동 dismiss | state-already_installed + success alert |
| 닫기 시 localStorage `pwa_install_dismissed` 30일 | "30일간 보지 않기" 버튼 |

## 2. 상태 매트릭스 (5)

| 키 | state | 디바이스/조건 | 카드 강조 |
|---|-------|------------|---------|
| 1 | ios | iOS Safari 16.4+ | iOS 카드 |
| 2 | android | Android Chrome 80+ | Android 카드 |
| 3 | desktop | Desktop (any) | Desktop 카드 |
| 4 | ios_old | iOS Safari < 16.4 | (강조 없음 + warning alert) |
| 5 | already_installed | `window.matchMedia('(display-mode: standalone)')` | (강조 없음 + success alert) |

## 3. 디자인 시스템 사용

- 레이아웃: `.auth-shell` / `.auth-card.auth-card-wide` (720px)
- 컴포넌트: `.install-grid` + `.install-card` (× 3 디바이스, 디자인 시스템 정의)
- 폼: `.btn-primary` / `.btn-ghost`
- 알림: `.auth-alert` (warning/success)
- 페이지 한정: `.install-card-iOS / -Android / -Desktop` opacity + active 강조 / `.device-detection` (UA 표시) / `.state-debug`

## 4. i18n 매핑

| 키 | ko | en |
|----|----|-----|
| `install.title` | FlowHR 앱 설치 | Install FlowHR |
| `install.subtitle` | 홈 화면에 설치하면 푸시 알림을 받고… | Install to home screen for push notifications… |
| `install.ios.step1` | 하단의 공유 버튼 탭 | Tap the Share button |
| `install.ios.step2` | "홈 화면에 추가" 선택 | Select "Add to Home Screen" |
| `install.ios.note` | iOS 16.4+ 필요 · 푸시 알림 가능 | Requires iOS 16.4+ · Push enabled |
| `install.android.step1` | 우측 상단 ⋮ 메뉴 | Top right ⋮ menu |
| `install.android.step2` | "앱 설치" 또는 자동 배너 탭 | "Install app" or tap auto banner |
| `install.desktop.title` | Desktop (Tauri 권장) | Desktop (Tauri recommended) |
| `install.dismiss_30d` | 30일간 보지 않기 | Don't show for 30 days |
| `install.ios_old.title` | iOS 16.4 이상이 필요합니다 | Requires iOS 16.4 or later |
| `install.already.title` | 이미 설치된 앱으로 진입하셨습니다 | Already opened as installed app |

## 5. 디바이스 감지 매핑 (02-device-matrix.md)

```js
function detectDevice() {
  const ua = navigator.userAgent;
  const isStandalone = window.matchMedia('(display-mode: standalone)').matches
    || window.navigator.standalone; // iOS legacy
  if (isStandalone) return 'already_installed';

  const isIOS = /iPad|iPhone|iPod/.test(ua);
  if (isIOS) {
    const ver = parseInt((ua.match(/OS (\d+)_/) || [])[1] || '0', 10);
    return ver >= 16 ? 'ios' : 'ios_old'; // 단순화 — 16.4 정확 비교는 별도
  }

  const isAndroid = /Android/.test(ua);
  if (isAndroid) return 'android';

  return 'desktop';
}
```

## 6. 검증

- [ ] 5 상태 토글 시 device-label 텍스트 변경
- [ ] 활성 디바이스 카드만 opacity:1 + accent border + shadow
- [ ] ios_old / already_installed 상태에서 alert 노출
- [ ] "30일간 보지 않기" 클릭 — localStorage 시뮬 (Phase 7 실제 구현)
- [ ] 모바일 ≤768px — install-grid 1 column

## 7. Phase 7 변환

- Next.js: middleware에서 device 감지 → `/install` redirect (조건부)
- `beforeinstallprompt` 이벤트 (Android) — 자동 배너 표시
- iOS — 공유 메뉴 직접 안내 (beforeinstallprompt 미지원)
- Tauri Releases 링크는 환경 변수 `NEXT_PUBLIC_TAURI_RELEASES_URL`
- localStorage TTL — 직접 비교 또는 `idb-keyval`

## 권한 매트릭스 (audit hotfix1 — KI-049 확대 보강)

| 역할 | 권한 |
|------|------|
| 모든 사용자 (PWA 설치 안내, 비인증 진입 허용) | — |

자세한 권한은 PRD §2 (사용자·권한 섹션) 참조. RLS 정책은 Phase 3 `rls.md` SSOT 정합.
