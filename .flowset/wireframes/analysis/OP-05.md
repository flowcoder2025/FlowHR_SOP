# OP-05 구독/요금제 관리 — Wireframe Analysis

> **소스**: `wireframes/html/OP-05.html`
> **PRD**: `prd/domains/operator/OP-05-subscriptions.md`
> **레이아웃**: `_design-system/_layout-shell.html` (active=구독/요금제)
> **패턴**: List + Top Filter + Create/Edit Modal
> **작성**: 2026-05-16 (G2 wf-v0.2.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| Plan 마스터 관리 (CRUD) | 테이블 5 행 (기본/프리미엄/엔터프라이즈/legacy/커스텀) |
| 8 컬럼 (요금제명/기본요금/인당/포함인원/모듈/구독수/상태/액션) | colgroup width 분배 + 모듈 chip + 우측 정렬 가격 |
| 4 필터 (상태/공개/유형/검색) | 상단 filter-row select 3종 + 초기화 |
| "요금제 생성" CTA | btn-primary + i-plus → create_modal |
| 플랜 모달 (입력 + 모듈 토글 + 공개여부) | 2-col grid + 모듈 4-col checkbox + 공개 라디오 |
| 사용 중 플랜 수정 시 가격 변경 다음 청구일 적용 안내 | edit_modal warning banner |

## 2. 상태 매트릭스 (5)

| 키 | state |
|---|-------|
| 1 | default (활성 5 행) |
| 2 | create_modal (신규 생성 모달) |
| 3 | edit_modal (수정 모달 + 사용 중 안내) |
| 4 | inactive_view (비활성/legacy 포함, opacity 1) |
| 5 | empty (생성된 플랜 없음 hero) |

## 3. i18n 키

| 키 | ko |
|----|----|
| `screens.op-05.title` | 구독 / 요금제 관리 |
| `screens.op-05.action.create` | 요금제 생성 |
| `screens.op-05.col.basicFee` | 기본요금/월 |
| `screens.op-05.col.perUser` | 인당/월 |
| `screens.op-05.col.modules` | 제공 모듈 |
| `screens.op-05.modal.create_title` | 새 요금제 생성 |
| `screens.op-05.modal.edit_warning` | 이 플랜은 현재 {count}개 테넌트가 구독 중입니다 |

## 4. API

```
GET    /api/v1/operator/plans
POST   /api/v1/operator/plans
PATCH  /api/v1/operator/plans/:id
DELETE /api/v1/operator/plans/:id              # = deactivate
POST   /api/v1/operator/plans/:id/clone
```

## 5. 검증

- [ ] 5 상태 토글
- [ ] 가격 우측 정렬 + tabular-nums
- [ ] module-chip 6+종 inline
- [ ] inactive 행 opacity 0.45 (state-default) / 1 (state-inactive_view)
- [ ] 수정 모달 — 슬러그 disabled + warning banner

## 6. Phase 7 변환

- shadcn/ui Dialog + Form (RHF + zod `planSchema`)
- 슬러그 변경 불가 (입력 disabled + Phase 7 react-hook-form `disabled` prop)
- 비활성화 처리 — soft delete (status=inactive), 새 구독 차단 + 기존 유지

## 권한 매트릭스 (KI-049 보강 + KI-069 표 구조 정정)

| 역할 | 권한 |
|------|------|
| operator_super, operator_billing | 요금제 CRUD |
| operator_cs | 조회 (R) |

자세한 권한은 PRD §2 (사용자·권한 섹션) 참조. RLS 정책은 Phase 3 `rls.md` SSOT 정합.
