# TA-09 결재 / 승인 — Wireframe Analysis

> **소스**: `wireframes/html/TA-09.html`
> **PRD**: `prd/domains/tenant-admin/TA-09-approvals.md`
> **레이아웃**: `_design-system/_layout-shell.html` (AppShell + tenant 8 메뉴, active=결재)
> **패턴**: Master-Detail Inbox (component-usage-matrix.json §Master-Detail)
> **작성**: 2026-05-17 (G3 wf-v0.3.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| 경로 `/admin/approvals` | sidebar active=결재 + page-title="결재 / 승인" |
| 인박스 사이드바 (받은/보낸/위임/완료) | `.inbox-tabs` 4 tab — `.is-active` variant |
| 받은 결재 카운트 chip | warning chip (대기 5) |
| 위임받은 (v1.1) | tab disabled + small "v1.1" |
| 테이블 / 카드 리스트 (PWA 최적화) | `.approval-row` (PRD §3-2 9컬럼 → 카드 응축) |
| 일괄 처리 (같은 유형만) | page-action-bar "일괄 승인 (선택 N)" |
| 우측 상세 + 결재 라인 + 액션 | 우측 `.card` (TA-08 요약 + 결재라인 + 승인/반려/의견) |
| 상세 화면 진입 링크 | "상세 화면 (TA-08)으로 이동" link |

## 2. 상태 매트릭스 (5)

| 키 | state | 설명 | 컨텐츠 분기 |
|---|-------|------|------------|
| 1 | default | 받은 결재 5건 (휴가 3 · 근태 2) | 받은 탭 is-active, 첫 행 is-active |
| 2 | loading | 인박스 fetch | approval-shell opacity 0.4 + spinner |
| 3 | filtered | 보낸 결재 3건 | 보낸 탭 is-active, 보낸 목록 표시 |
| 4 | empty | 받은 결재 0 | empty-state 양쪽 (목록 + 상세) |
| 5 | error | APPROVALS_FETCH_FAILED | 좌측 alert + req_i7j0 |

## 3. 디자인 시스템 사용

- 레이아웃: `.app-shell` + `.sidebar` (active=결재) + `.header` + `.content` + `.footer`
- 컴포넌트:
  - `.page-action-bar` + `.summary` + `.actions`
  - `.card` + `.inbox-sidebar` (no padding 변종) + 우측 `.card`
  - `.badge.badge-info/warning/success/muted`
  - `.btn.btn-primary/btn-danger/btn-ghost`
  - `.empty-state` + i-check
  - `.auth-alert.auth-alert-error`
- 페이지 한정: `.approval-shell` (380px / 1fr grid) / `.inbox-tabs` / `.inbox-tab.is-active` / `.approval-row.is-active` (3px accent border-left) / `.count-chip` warning / `.row-head` / `.row-title` / `.row-meta` / `.loading-overlay` / `.state-debug`

## 4. i18n 매핑

| 키 | ko | en |
|-----|----|-----|
| `screens.ta-09.title` | 결재 / 승인 | Approvals |
| `screens.ta-09.tab.inbox` | 받은 | Inbox |
| `screens.ta-09.tab.sent` | 보낸 | Sent |
| `screens.ta-09.tab.delegated` | 위임 | Delegated |
| `screens.ta-09.tab.done` | 완료 | Done |
| `screens.ta-09.action.batchApprove` | 일괄 승인 | Batch Approve |
| `screens.ta-09.detail.balanceBefore` | 잔여 (현재) | Balance (current) |
| `screens.ta-09.detail.balanceAfter` | 승인 후 잔여 | Balance (after) |
| `screens.ta-09.detail.substitute` | 대체근무 | Substitute |
| `screens.ta-09.empty.allDone` | 처리할 결재가 없습니다 | All approvals done |

## 5. API 매핑

```
GET  /api/v1/tenant/approvals/inbox?type&status&page
GET  /api/v1/tenant/approvals/sent?type&status&page
GET  /api/v1/tenant/approvals/:id
POST /api/v1/tenant/approvals/:id/approve
POST /api/v1/tenant/approvals/:id/reject
POST /api/v1/tenant/approvals/:id/comment
POST /api/v1/tenant/approvals/:id/cancel
POST /api/v1/tenant/approvals/batch-approve     # 같은 type만
```

## 6. 권한 매트릭스 (Gherkin 정합)

- `tenant_super` / `tenant_hr_admin`: R/A/N 전체
- `tenant_manager`: R/A 자기 단계만 (지정된 결재만)
- `employee`: C/R/Cancel 본인
- 위임 (v1.1): 임시 위임된 결재는 `위임` 탭에 표시

## 7. 검증 (시각 검수)

- [ ] 5 상태 토글 — 받은 / loading / 보낸 / 빈 / 오류
- [ ] 인박스 탭 — is-active variant (`.active` 금지) + count chip warning
- [ ] 위임 탭 disabled (v1.1 small)
- [ ] 행 선택 — 좌측 accent border + 우측 상세 동기화
- [ ] SLA 임박 행 — badge-warning "SLA 1h" / "23m"
- [ ] 우측 상세 — 신청자 / 유형기간 / 잔여 / 대체 / 사유 / 결재라인 / 액션
- [ ] 결재 라인 — 현재 단계 badge-warning, 이후 단계 muted
- [ ] 액션 — 승인(primary) / 반려(danger) / 의견(ghost icon)
- [ ] empty 양쪽 — 좌측 small empty + 우측 main empty

## 8. Phase 7 변환

- shadcn/ui ResizablePanel (좌/우 width 조절)
- PWA Sheet 변환 (≤768px 모바일 — 좌측 인박스 풀스크린 → 행 클릭 시 우측 슬라이드 인)
- 스와이프 액션 (좌 = 승인 / 우 = 반려) — react-swipeable
- 일괄 승인 — 체크박스 다중 선택 + 같은 유형만 (zod validation)
- Realtime: `approvals:tenant_id={id}` channel — 신규 / 단계 전이 / SLA 임박 알림

## 9. 의존성

- **선행**: EM-03 / EM-08 / TA-06 (요청 생성)
- **연계**: TA-08 (휴가 상세), TA-06 (근태 상세)
- **이벤트**: 단계 전이 / 신규 / SLA 임박 시 Realtime broadcast + 알림 채널 폴백
