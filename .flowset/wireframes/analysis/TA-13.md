# TA-13 회사 설정 — Wireframe Analysis

> **소스**: `wireframes/html/TA-13.html`
> **PRD**: `prd/domains/tenant-admin/TA-13-settings.md`
> **레이아웃**: `_design-system/_layout-shell.html` (AppShell + tenant 8 메뉴, active=설정)
> **패턴**: Vertical Tabs Settings (component-usage-matrix.json §Vert-Tabs)
> **작성**: 2026-05-17 (G3 wf-v0.3.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| 경로 `/admin/settings` | sidebar active=설정 + page-title="회사 설정" |
| 탭 9 + 9 pane stack | `.vert-tabs` + `.vert-tab` 9개 + 본문 9 pane (`.card.pane` × 9, hotfix1 추가) |
| 1. 회사정보 pane (scope-hr) | input (회사명/대표자/연락처/이메일/주소) + select-wrap (업종) + file-input wrap (로고) |
| 2. 근무정책 pane (scope-hr) | select-wrap × 4 (출퇴근/휴게/지각/52h) + radio (근무제 4) + toggle (52h 알림) + date-input (적용 시점) |
| 3. 휴가정책 pane (scope-hr) | radio (부여 기준) + table (leave_types 5종) + "유형 추가" 액션 |
| 4. 결재라인 pane (scope-hr, R) | table (5 종류 + 조건 + 단계 + SLA + 액션) + "라인 추가" |
| 5. 역할권한 pane (super only) | i-lock 아이콘 + 권한 매트릭스 테이블 (super/hr_admin/manager/employee × 7 항목) |
| 6. 알림 pane (scope-hr) | select-wrap (1순위/2순위/폴백 임계) + 10 템플릿 링크 |
| 7. 문서양식 pane (scope-hr) | table (5 양식 + 변수 + 버전 + 수정) + "양식 업로드" |
| 8. 보안 pane | form-row × 6 (비밀번호/세션/2FA/IP 등) + empty state에서 auth-alert-info "수정 권한 없음" 표시 |
| 9. 감사로그 pane (본 테넌트 한정) | table (시각/처리자/이벤트/대상) + "전체 보기" link (OP-09는 운영사 전용) |
| 적용 예정 시점 (적용일 선택) | date-input + "저장 + 적용예정" 액션 바 |
| 변경 이력 액션 | page-action-bar btn-ghost + i-history |

## 2. 상태 매트릭스 (5)

| 키 | state | 설명 | 컨텐츠 분기 |
|---|-------|------|------------|
| 1 | default | super 전권 — 9 pane 모두 stack 표시 | `body[data-state="default"] .pane { display: block; }` + vert-tab[data-tab="work"] active 강조 |
| 2 | loading | 조회 중 | pane-canvas opacity 0.4 + spinner |
| 3 | filtered | hr_admin — 6 pane (회사정보/근무/휴가/결재라인/알림/문서양식, scope-hr 부착) | `body[data-state="filtered"] .pane.scope-hr { display: block; }` + vert-tab[data-tab="leave"] active 강조 |
| 4 | empty | 보안 탭 — hr_admin read-only 진입 시나리오 | vert-tab[data-tab="security"] active + pane-security 단독 + auth-alert-info "수정 권한 없음" |
| 5 | error | SETTINGS_FETCH_FAILED | auth-alert-error + req_m1n4 |

## 3. 디자인 시스템 사용

- 레이아웃: `.app-shell` + `.sidebar` (active=설정) + `.header` + `.content` + `.footer`
- 컴포넌트:
  - `.page-action-bar`
  - `.vert-tabs` + `.vert-tab` (variant `.is-active`는 body[data-state] CSS로 토글)
  - `.card` + `.form-section` + `.form-section-header` + `.form-row` + `.label` + `.select` + `.select-wrap` + `.toggle` + `.toggle-slider` + `.date-input` + `.input`
  - `.table` (휴가 유형 마스터)
  - `.badge.badge-muted` (read-only 표시)
  - `.btn.btn-primary/btn-ghost/btn-sm` + i-save / i-plus / i-history
  - `.auth-alert.auth-alert-info` (권한 안내) + `.auth-alert.auth-alert-error`
- 페이지 한정: `.settings-shell` (220px / 1fr grid) / `.pane-canvas` / pane 토글 / `.loading-overlay` / `.state-debug`

## 4. i18n 매핑

| 키 | ko | en |
|-----|----|-----|
| `screens.ta-13.title` | 회사 설정 | Company Settings |
| `screens.ta-13.tab.company` | 회사정보 | Company |
| `screens.ta-13.tab.work` | 근무정책 | Work Policy |
| `screens.ta-13.tab.leave` | 휴가정책 | Leave Policy |
| `screens.ta-13.tab.approval` | 결재라인 | Approval Lines |
| `screens.ta-13.tab.role` | 역할권한 | Roles |
| `screens.ta-13.tab.noti` | 알림 | Notifications |
| `screens.ta-13.tab.template` | 문서양식 | Templates |
| `screens.ta-13.tab.security` | 보안 | Security |
| `screens.ta-13.tab.audit` | 감사로그 | Audit Log |
| `screens.ta-13.work.standard` | 표준 근무시간 | Standard Hours |
| `screens.ta-13.work.lateThreshold` | 지각 기준 | Late Threshold |
| `screens.ta-13.leave.types` | leave_types 마스터 | Leave Types |
| `screens.ta-13.security.readOnly` | 수정 권한이 없습니다 | No edit permission |

## 5. API 매핑

```
GET   /api/v1/tenant/settings                       # 모든 탭 메타
PATCH /api/v1/tenant/settings/{tab}                 # 탭별 저장 (적용 예정)
GET   /api/v1/tenant/work-policies
POST  /api/v1/tenant/work-policies
GET   /api/v1/tenant/leave-types
POST  /api/v1/tenant/leave-types
GET   /api/v1/tenant/approval-lines
POST  /api/v1/tenant/approval-lines
GET   /api/v1/tenant/document-templates
POST  /api/v1/tenant/document-templates
GET   /api/v1/tenant/audit-logs                     # 본 테넌트만
```

## 6. 권한 매트릭스 (Gherkin 정합)

- `tenant_super`: CRUS/L 전권 9 탭
- `tenant_hr_admin`: R/U 일부 (회사정보 / 휴가정책 / 문서양식 수정 / 권한·보안 read-only)
- `tenant_manager`: R 일부 (조회만)
- `employee`: R 일부 (취업규칙 / 휴가정책 등 공개 정보만)

## 7. 검증 (시각 검수)

- [ ] 5 상태 토글 — 근무 / loading / 휴가 / 보안 read-only / 오류
- [ ] vert-tabs 9 (보안 탭에 i-lock)
- [ ] 탭 active variant `.is-active`만 사용 (`.active` 금지) — body[data-state] CSS 토글
- [ ] 근무정책 — select-wrap (출퇴근시간), radio (근무제), toggle (52h 알림), date-input (적용일)
- [ ] 휴가정책 — leave_types 5종 테이블 + 유형 추가 액션
- [ ] 보안 (empty) — auth-alert-info 안내 + 모든 폼 read-only 표시
- [ ] error 상태 — auth-alert-error + req_m1n4

## 8. Phase 7 변환

- shadcn/ui Tabs (orientation=vertical) + URL ?tab=X
- react-hook-form + zod (탭별 schema)
- 적용 예정 — `scheduled` 상태 + cron 변환 (예: 매일 00:00 status=scheduled→active)
- 감사로그 — TA-13 audit_logs 탭 (Supabase RLS `tenant_id`)
- 권한 게이팅 hook — 각 탭별 R/U 매트릭스
- 휴가 유형 편집 — shadcn/ui Dialog + LeaveType form

## 9. 의존성

- **선행**: OP-04 초기 세팅 완료
- **연계**: TA-02 (직원 권한), TA-05 (work_policy), TA-07 (leave_types), TA-09 (approval_lines), TA-10/11 (templates), CM-15 (알림)
- **이벤트**: 정책 변경 시 영향 받는 화면 즉시 갱신 + 직원 알림
