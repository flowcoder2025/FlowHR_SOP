# TA-11 문서함 / 전자계약 — Wireframe Analysis

> **소스**: `wireframes/html/TA-11.html`
> **PRD**: `prd/domains/tenant-admin/TA-11-contracts.md`
> **레이아웃**: `_design-system/_layout-shell.html` (AppShell + tenant 8 메뉴, active=급여/문서)
> **패턴**: List + Filter Bar + 7 Cols Table + v1.2 안내 (MVP partial)
> **작성**: 2026-05-17 (G3 wf-v0.3.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| 경로 `/admin/contracts` | sidebar active=급여/문서 (sub-route) + page-title="문서함 / 전자계약" |
| MVP partial — PDF 업로드/다운로드만 | 상단 auth-alert-info 배너 "v1.2 출시 예정" + 본문 흐름 안내 |
| 테이블 7 컬럼 (계약서명/대상자/템플릿/발송상태/요청일/완료일/액션) | colgroup width 분배 |
| 필터 (서명상태/문서유형/대상자/기간) | filter-bar 5 chip — "서명 대기" v1.2 small + i-lock |
| 계약서 생성 모달 흐름 | 본문 하단 카드 "계약서 생성 흐름" 4 단계 안내 |
| 템플릿 관리 액션 | btn-ghost + i-edit |
| 전자서명 (v1.2) | filter-bar 칩 + filtered 상태 안내 |

## 2. 상태 매트릭스 (5)

| 키 | state | 설명 | 컨텐츠 분기 |
|---|-------|------|------------|
| 1 | default | 전체 38건 | table 5행 |
| 2 | loading | 조회 중 | table opacity 0.4 |
| 3 | filtered | 서명 대기 = v1.2 Coming soon | filtered 안내 패널 (i-lock + 안내문) |
| 4 | empty | 계약서 0 | empty-state + i-file-text |
| 5 | error | CONTRACTS_FETCH_FAILED | auth-alert-error + req_k9l2 |

## 3. 디자인 시스템 사용

- 레이아웃: `.app-shell` + `.sidebar` (active=급여/문서) + `.header` + `.content` + `.footer`
- 컴포넌트:
  - `.auth-alert.auth-alert-info` (v1.2 안내 배너) + `.auth-alert.auth-alert-error`
  - `.filter-bar` + `.filter-chip.is-active`
  - `.page-action-bar`
  - `.card.card-flat` + `.table` + colgroup
  - `.card` + `.card-title` (생성 흐름 안내)
  - `.badge.badge-success/warning`
  - `.btn.btn-primary/btn-ghost/btn-sm` + i-edit / i-plus / i-send
  - `.icon-btn.icon-btn-sm` + i-more
  - `.empty-state` + i-file-text / i-lock (v1.2)
- 페이지 한정: `.loading-overlay` / `.state-debug`

## 4. i18n 매핑

| 키 | ko | en |
|-----|----|-----|
| `screens.ta-11.title` | 문서함 / 전자계약 | Documents / e-Contracts |
| `screens.ta-11.banner.v12` | 전자서명은 v1.2 출시 예정입니다 | e-Signature available in v1.2 |
| `screens.ta-11.action.template` | 템플릿 관리 | Templates |
| `screens.ta-11.action.create` | 계약서 생성 | Create Contract |
| `screens.ta-11.col.contractName` | 계약서명 | Contract Name |
| `screens.ta-11.col.template` | 템플릿 | Template |
| `screens.ta-11.col.requestDate` | 요청일 | Request Date |
| `screens.ta-11.col.completedDate` | 완료일 | Completed Date |
| `screens.ta-11.flow.title` | 계약서 생성 흐름 (MVP) | Contract Creation Flow (MVP) |

## 5. API 매핑

```
GET   /api/v1/tenant/contracts?status&type&page
POST  /api/v1/tenant/contracts                  # 신규 (템플릿 + 변수)
GET   /api/v1/tenant/contracts/:id
POST  /api/v1/tenant/contracts/:id/send
POST  /api/v1/tenant/contracts/:id/preview      # PDF
GET   /api/v1/tenant/contracts/:id/download
POST  /api/v1/tenant/contracts/:id/request-sign # v1.2
POST  /api/v1/tenant/contracts/:id/sign         # v1.2 (직원)
```

## 6. 권한 매트릭스 (Gherkin 정합)

- `tenant_super`: CRUD/E/N
- `tenant_hr_admin`: CRU/E/N
- `tenant_manager`: R 자기 팀 일부 (메타만, 본문 비표시)
- `employee`: R/Sign 본인 (v1.2 서명)

## 7. 검증 (시각 검수)

- [ ] 5 상태 토글 — 전체 / loading / v1.2 안내 / 빈 / 오류
- [ ] v1.2 안내 배너 (auth-alert-info)
- [ ] filter-bar "서명 대기" v1.2 small + i-lock
- [ ] 테이블 7 컬럼 + 미발송 행 warning-bg + 발송 버튼
- [ ] 배지 — 수령확인(success) / 발송(success) / 미발송(warning)
- [ ] 생성 흐름 안내 카드 (4단계)
- [ ] empty / error 상태

## 8. Phase 7 변환

- shadcn/ui Dialog (계약서 생성 모달) + react-hook-form
- 템플릿 선택 — DocumentTemplate dropdown + 변수 자동 채움 (employees fetch)
- PDF 생성 — React-PDF 또는 Puppeteer + Signed URL
- 전자서명 (v1.2) — 모두싸인 API integration + 이메일/SMS OTP
- 진행 상태 Realtime: `signatures:contract_id={id}` channel

## 9. 의존성

- **선행**: TA-13 (DocumentTemplate 등록), TA-02 (직원)
- **연계**: TA-10 (전체 문서 관리), EM-07 (직원 조회)
- **외부 v1.2**: 전자서명 인증사업자 API (모두싸인 / 헬로싸인)
