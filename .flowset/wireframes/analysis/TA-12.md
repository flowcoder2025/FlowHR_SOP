# TA-12 리포트 — Wireframe Analysis

> **소스**: `wireframes/html/TA-12.html`
> **PRD**: `prd/domains/tenant-admin/TA-12-reports.md`
> **레이아웃**: `_design-system/_layout-shell.html` (AppShell + tenant 8 메뉴, active=리포트)
> **패턴**: Left Report List + Right Chart Canvas (Master-Detail 변종)
> **작성**: 2026-05-17 (G3 wf-v0.3.0)

## 1. PRD 매핑

| PRD 항목 | 구현 |
|---------|------|
| 경로 `/admin/reports` | sidebar active=리포트 + page-title="리포트" |
| 좌측 리포트 카드 5 + 커스텀(v1.3) | `.report-list` + `.report-item` is-active/is-disabled |
| 필터 (기간/부서/직급/고용형태/기간비교) | filter-bar 5 chip |
| 인력 — 인원 추이 Line + 부서 분포 Donut + 증감 테이블 | chart-grid 2-col + wide 행 |
| 근태 — 일별 출근율 Line + 지각/결근 Stacked | (Phase 7 확장) |
| 휴가 — 직원별 사용 Bar + 유형별 Donut | (TA-07 KPI 보완) |
| 초과근무 — 주 52시간 모니터링 + 임박자 알림 | filtered 상태 — warning 카드 + 알림 발송 버튼 + 주별 추이 + 초과 인원 추이 |
| 부서별 비교 Radar | (Phase 7) |
| PDF / Excel 내보내기 | btn-ghost + i-pdf / i-excel |
| 커스텀 리포트 (v1.3) | is-disabled + i-lock + v1.3 small |

## 2. 상태 매트릭스 (5)

| 키 | state | 설명 | 컨텐츠 분기 |
|---|-------|------|------------|
| 1 | default | 인력 리포트 | report-item[data-report="headcount"] active + chart-grid 표시 |
| 2 | loading | 집계 중 | chart-grid opacity 0.4 + spinner |
| 3 | filtered | 초과근무 리포트 | report-item[data-report="overtime"] active + 임박 warning 카드 + 52h 추이 |
| 4 | empty | 집계 데이터 없음 | empty-state + i-chart |
| 5 | error | REPORT_FETCH_FAILED | auth-alert-error + req_l0m3 |

## 3. 디자인 시스템 사용

- 레이아웃: `.app-shell` + `.sidebar` (active=리포트) + `.header` + `.content` + `.footer`
- 컴포넌트:
  - `.filter-bar` + `.filter-chip.is-active`
  - `.page-action-bar` + `.actions`
  - `.card` (report-list wrapper + chart cards) + `.section-header` + `.card-wide`
  - `.table` (증감 테이블)
  - `.badge.badge-warning`
  - `.btn.btn-primary/btn-ghost/btn-sm`
  - `.empty-state` + i-chart
  - `.auth-alert.auth-alert-error`
- 페이지 한정: `.report-shell` (260px / 1fr grid) / `.report-list` / `.report-item.is-active/is-disabled` / `.chart-grid` (2-col + wide) / `.loading-overlay` / `.state-debug`

## 4. i18n 매핑

| 키 | ko | en |
|-----|----|-----|
| `screens.ta-12.title` | 리포트 | Reports |
| `screens.ta-12.report.headcount` | 인력 | Headcount |
| `screens.ta-12.report.attendance` | 근태 | Attendance |
| `screens.ta-12.report.leave` | 휴가 | Leave |
| `screens.ta-12.report.overtime` | 초과근무 | Overtime |
| `screens.ta-12.report.deptCompare` | 부서별 비교 | Department Compare |
| `screens.ta-12.report.custom` | 커스텀 리포트 | Custom Report |
| `screens.ta-12.compare.label` | 전월 / 전년 동기 비교 | YoY / MoM Compare |
| `screens.ta-12.action.pdf` | PDF 다운로드 | Download PDF |
| `screens.ta-12.action.excel` | Excel 내보내기 | Export Excel |
| `screens.ta-12.overtime.imminent` | 52시간 임박 | 52h Imminent |
| `screens.ta-12.overtime.notify` | 알림 발송 | Notify |

## 5. API 매핑

```
GET  /api/v1/tenant/reports/headcount?from&to&dept
GET  /api/v1/tenant/reports/attendance?from&to&dept
GET  /api/v1/tenant/reports/leaves?from&to&dept&type
GET  /api/v1/tenant/reports/overtime?from&to&dept
GET  /api/v1/tenant/reports/department-comparison?from&to
POST /api/v1/tenant/reports/export/pdf?type
POST /api/v1/tenant/reports/export/excel?type
```

## 6. 권한 매트릭스 (Gherkin 정합)

- `tenant_super` / `tenant_hr_admin`: R/E (전체)
- `tenant_manager`: R 소속팀 일부 (부서 필터 자동 고정 + 부서비교 비활성)
- `employee`: R 개인 일부 (EM-04 잔여만 — 본 화면 비표시)
- 커스텀 리포트 (v1.3): is-disabled + Coming soon

## 7. 검증 (시각 검수)

- [ ] 5 상태 토글 — 인력 / loading / 초과근무 / 빈 / 오류
- [ ] 좌측 카드 6 (5 + 1 v1.3) — is-active variant + is-disabled + SLA 배지 (초과근무 옆)
- [ ] 필터 5 chip
- [ ] 우측 인력 — 인원 추이 (12M Line) + 부서 분포 Donut (4색) + 증감 테이블 (wide)
- [ ] 초과근무 — 임박 warning 카드 (52h 미만, alert-t) + 주별 추이 + 초과 인원 추이
- [ ] 50h dashed 임계선 + "52h 임계" 라벨
- [ ] empty / error 상태

## 8. Phase 7 변환

- recharts / visx (LineChart / BarChart / DonutChart / Radar / StackedBar)
- 머터리얼라이즈드 뷰 (성능) — 부서별 집계 사전 계산
- PDF 생성: Puppeteer (서버) → Signed URL
- Excel: SheetJS (서버)
- 커스텀 리포트 (v1.3): 빌더 UI + saved queries
- 권한 미들웨어: manager는 `dept_id` 자동 필터 + 부서비교 disable

## 9. 의존성

- **선행**: TA-02 / TA-05 / TA-07 (집계 원본 데이터)
- **연계**: OP-10 운영사 리포트 (별도)
- **성능**: 대용량 집계 — 부서별 머터리얼라이즈드 뷰 검토
